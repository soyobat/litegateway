<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.grace.gateway.admin.mapper.GatewayInstanceMapper">

    <!-- 查询所有网关实例 -->
    <select id="selectList" resultType="com.grace.gateway.admin.entity.GatewayInstanceEntity">
        SELECT
            id, name, ip, port, status, version, start_time, config_id, config_name
        FROM
            gateway_instance
        ORDER BY
            start_time DESC
    </select>

    <!-- 根据ID查询网关实例 -->
    <select id="selectById" resultType="com.grace.gateway.admin.entity.GatewayInstanceEntity">
        SELECT
            id, name, ip, port, status, version, start_time, config_id, config_name
        FROM
            gateway_instance
        WHERE
            id = #{id}
    </select>

</mapper>
